// Relative paths keep the site portable under a GitHub Pages repository path.
// No external service, analytics, credentials, or author identifiers belong here.
export default Object.freeze({
  startupExample: "compact_loop",
  playbackSeconds: 6,
  inferenceOptions: Object.freeze({
    profile: "fast_v1",
  }),
});
